package g5.hotelreservationwebapp.backend.repository;

import g5.hotelreservationwebapp.backend.model.CustomerRoomReservationFeedback;
import org.springframework.stereotype.Repository;

@Repository
public interface CustomerRoomReservationFeedbackRepository extends BaseEntityRepository<CustomerRoomReservationFeedback> {
    
}